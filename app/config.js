
import { levelParams } from './level';

let config = {
    "buildDir": "./build",
    "stageWidth": levelParams.stageWidth,
    "stageHeight": levelParams.stageHeight,
    "ammo": {
        "bullet": "fire.png"
    }
};


export { config };
